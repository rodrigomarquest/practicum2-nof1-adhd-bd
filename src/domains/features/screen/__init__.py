"""Screen feature engineering package

Default input: snapshot dir (data/etl/<PID>/<SNAPSHOT>/). Reads screen-related
artifacts and can augment the joined dataset.

Default output: per-domain CSVs under `joined/` or `processed/`.
"""

__all__ = []

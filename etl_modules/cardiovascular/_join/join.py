"""Cross-platform join rules (stub)."""
import pandas as pd
def join_hr(hr_sources: list[pd.DataFrame]) -> pd.DataFrame | None: return None
def select_hrv(hrv_sources: list[pd.DataFrame]) -> pd.DataFrame | None: return None


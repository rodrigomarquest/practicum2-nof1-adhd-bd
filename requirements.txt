numpy>=2.3
pandas>=2.3
matplotlib>=3.8
python-dateutil>=2.9
pyarrow>=17
tqdm>=4.66
rich>=13
pywin32>=306 ; sys_platform == "win32"
# Practicum2 – unified root requirements
-r requirements_etl.txt
# Instalar ios só quando necessário, no venv de extração:
# -r requirements_ios.txt

# Shared utilities (podem duplicar com ETL sem problema)
python-dotenv>=1.0.1
tqdm>=4.66.5

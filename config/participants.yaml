# Participant registry for the N-of-1 pipeline.
# Each entry defines a participant ID, aliases, and ETL paths for snapshots.
# The pipeline uses this file to resolve which folders to read/write for each PID.

participants:
  - id: P000001
    label: primary
    tz: Europe/Dublin
    consent: self
  - id: P000002
    label: secondary
    tz: America/Sao_Paulo
    consent: external # ensure written consent stored offline
    nt: self
  - id: P000003
    label: secondary
    tz: America/Sao_Paulo
    consent: external # ensure written consent stored offline
    nt: self

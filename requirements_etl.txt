# Core ETL – Windows + Python 3.13 (safe)
numpy>=2.1
pandas>=2.2.3
python-dateutil>=2.9.0.post0
pytz>=2024.1
tzlocal>=5.2

# QoL / logs
rich>=13.7.1
colorama>=0.4.6

# SQLite helpers (opcional mas útil p/ inspeções)
sqlite-utils>=3.36.0

# Visualização leve (opcional; remover se quiser ETL headless)
matplotlib>=3.9.0
	# Crypto primitives used by iOS backup helpers and AES operations
	# pycryptodome provides the `Crypto` top-level package used in etl_modules.iphone_backup
pycryptodome>=3.20.0

# Support AES-encrypted zip archives (Zepp exports may use AES encryption)
# Installed/pinned in this environment
pyzipper==0.3.6
